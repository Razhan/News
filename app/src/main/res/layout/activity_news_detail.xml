<?xml version="1.0" encoding="utf-8"?>
<!--suppress AndroidUnknownAttribute -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <import type="android.text.TextUtils"/>
        <import type="com.guanchazhe.news.mvp.model.entities.NewsType"/>

        <variable name="news" type="com.guanchazhe.news.mvp.model.entities.News"/>
        <variable name="presenter" type="com.guanchazhe.news.mvp.presenters.NewsDetailPresenter"/>
        <variable name="swatch" type="android.support.v7.graphics.Palette.Swatch"/>
    </data>

    <android.support.design.widget.CoordinatorLayout
        android:id="@+id/parent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <android.support.design.widget.AppBarLayout
            android:id="@+id/character_appbar"
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:fitsSystemWindows="true"
            android:theme="@style/AppTheme.AppBarOverlay">

            <ImageView
                android:id="@+id/character_image"
                android:layout_width="match_parent"
                android:layout_height="230dp"
                android:layout_gravity="center"
                android:scaleType="centerCrop"
                android:fitsSystemWindows="true"
                app:layout_scrollFlags="scroll|exitUntilCollapsed"
                presenter="@{presenter}"
                source="@{news.pic}"/>

            <TextView
                android:id="@+id/character_label_info"
                style="@style/Text.Header"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:paddingLeft="@dimen/spacing_normal"
                android:text="@{news.title}"
                android:textColor="@color/grey.light"/>

        </android.support.design.widget.AppBarLayout>

        <android.support.v4.widget.NestedScrollView
            android:id="@+id/character_nestedscroll"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <LinearLayout
                android:id="@+id/character_info_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <WebView
                    android:id="@+id/character_biography"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:lineSpacingExtra="8dp"
                    android:paddingLeft="@dimen/spacing_normal"
                    android:paddingRight="@dimen/spacing_normal"
                    android:scrollbars="none"
                    android:visibility="@{news != null ? View.VISIBLE : View.GONE}"/>

                <ProgressBar
                    android:id="@+id/activity_avenger_detail_progress"
                    style="?android:attr/progressBarStyleLarge"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_gravity="center_horizontal"
                    android:visibility="@{news == null ? View.VISIBLE : View.GONE}"/>
            </LinearLayout>
        </android.support.v4.widget.NestedScrollView>
    </android.support.design.widget.CoordinatorLayout>
</layout>